version: 2

# conda:
#     environment: install/environment.yml

build:
    os: ubuntu-latest
    tools:
        ptyhon: "3.8"

sphinx:
    configuration: docs/conf.py

formats:
  - pdf

python:
    version: 3.8
    install:
      - requirements: REQUIREMENTS
      - method: pip
        path: .
        extra_requirements:
         - docs
